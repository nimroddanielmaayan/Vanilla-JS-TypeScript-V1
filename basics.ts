// Primitives:

let age: number;

age = 12.1;

let userName: string;

userName = 'Max';

let isInstructor: boolean;

isInstructor = true;

// Complex types:

let hobbies: string[];

hobbies = ['Sports', 'Cooking'];

let person: {
  name: string;
  age: number;
};

person = {
  name: 'Max',
  age: 32,
};

let peopleArray: {
  name: string;
  age: number;
};
[];

// Any type (the default type):

let anything: any;

anything = 'Something';
